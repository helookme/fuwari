---
import { profileConfig } from "../config";
import { url } from "../utils/url-utils";

const currentYear = new Date().getFullYear();
---

<!--<div class="border-t border-[var(&#45;&#45;primary)] mx-16 border-dashed py-8 max-w-[var(&#45;&#45;page-width)] flex flex-col items-center justify-center px-6">-->
<div class="transition border-t border-black/10 dark:border-white/15 my-10 border-dashed mx-32"></div>
<!--<div class="transition bg-[oklch(92%_0.01_var(&#45;&#45;hue))] dark:bg-black rounded-2xl py-8 mt-4 mb-8 flex flex-col items-center justify-center px-6">-->
<div class="transition border-dashed border-[oklch(85%_0.01_var(--hue))] dark:border-white/15 rounded-2xl mb-12 flex flex-col items-center justify-center px-6">
    <div class="transition text-50 text-sm text-center">
        &copy; <span id="copyright-year">{currentYear}</span> {profileConfig.name}. All Rights Reserved. /
        <a class="transition link text-[var(--primary)] font-medium" target="_blank" href={url('rss.xml')}>RSS</a> /
        <a class="transition link text-[var(--primary)] font-medium" target="_blank" href={url('sitemap-index.xml')}>Sitemap</a><br>
        Powered by
        <a class="transition link text-[var(--primary)] font-medium" target="_blank" href="https://astro.build">Astro</a> &
        <a class="transition link text-[var(--primary)] font-medium" target="_blank" href="https://github.com/saicaca/fuwari">Fuwari</a>
    </div>
    <a href="https://icp.gov.moe/?keyword=20260130" target="_blank">萌ICP备20260130号</a>

    <!-- ===== CDN节点徽章（稳定版） ===== -->
    <div id="myCdnBadge" style="background: rgba(255,255,240,0.7); backdrop-filter: blur(4px); border-radius: 30px; padding: 8px 16px; display: inline-flex; align-items: center; gap: 8px; font-size: 14px; border: 1px solid rgba(0,0,0,0.1); margin: 10px 0;">
        <span style="font-size: 18px;">📡</span>
        <span id="cdnText">正在检测节点...</span>
    </div>
    
    <script>
        // 获取CDN节点信息（自动切换备用API）
        (function() {
            const cdnText = document.getElementById('cdnText');
            
            // 尝试多个API，直到成功
            function fetchWithFallback(apiIndex) {
                const apis = [
                    {
                        url: 'https://api.ip.sb/geoip',
                        parse: (data) => {
                            let location = data.country || '未知';
                            if (data.city) location = data.city + ', ' + location;
                            const org = data.isp || '未知运营商';
                            return `CDN节点：${location}<br><small>${org}</small>`;
                        }
                    },
                    {
                        url: 'https://ipapi.co/json/',
                        parse: (data) => {
                            let location = data.country_name || '未知';
                            if (data.city) location = data.city + ', ' + location;
                            const org = data.org || '未知运营商';
                            return `CDN节点：${location}<br><small>${org}</small>`;
                        }
                    }
                ];
                
                if (apiIndex >= apis.length) {
                    cdnText.innerText = 'CDN节点：未知';
                    return;
                }
                
                fetch(apis[apiIndex].url)
                    .then(res => res.json())
                    .then(data => {
                        cdnText.innerHTML = apis[apiIndex].parse(data);
                    })
                    .catch(() => {
                        // 当前API失败，尝试下一个
                        fetchWithFallback(apiIndex + 1);
                    });
            }
            
            fetchWithFallback(0);
        })();
    </script>
    <!-- ===== 结束 ===== -->

    <!-- ===== 广告位（修正版） ===== -->
    <div class="ad-footer" style="margin-top: 1.5rem; text-align: center; width: 100%;">
        <!-- 广告脚本（已添加 data-astro-reload 确保 Swup 切换后重新执行） -->
        <script data-astro-reload>
            atOptions = {
                'key' : 'a6c478fe233bd5ec2e2dc37de5088a9e',
                'format' : 'iframe',
                'height' : 300,
                'width' : 160,
                'params' : {}
            };
        </script>
        <script data-astro-reload src="https://www.highperformanceformat.com/a6c478fe233bd5ec2e2dc37de5088a9e/invoke.js"></script>
        
        <!-- 备用：如果 iframe 没有加载，显示一个简单提示（可选） -->
        <noscript>您的浏览器不支持 JavaScript，无法显示广告。</noscript>
    </div>
    <!-- ===== 广告位结束 ===== -->
    
</div>